## **parameter와 localparam 차이**
- 비트 폭을 명시적으로 할당 → 지정하지 않으면 Tool마다 해석이 달라짐
-  **parameter** : 모듈 외부에서 값 재정의가 가능한 상수
    → 데이터 폭, 크기 같은 설정값에 사용
-  **localparam** : 모듈 내부에서만 쓰는 변경 불가능한 상수
    → FSM 상태 등 고정 규칙에 사용
